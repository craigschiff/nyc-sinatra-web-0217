<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>
      Create a New Figure Below
    </h2>
    <form action="/figures" method="post">
      <label>Figure Name: </label>
      <input type="text" name="figure[name]" id="figure_name" /><br />
      <label>Select titles associated with the figure below </label><br />
      <% Title.all.each do |title| %>
      <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>" id="title_ids"><%= title.name %></input><br />
      <% end %>
      <label>And if a title is not listed please add below: </label><br />
      <input type="text" name="title[name]" id="new_title" /><br />
      <label>Select the landmarks associated with the figure  </label><br />
      <% Landmark.all.each do |landmark| %>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" id="landmark_ids"><%= landmark.name %></input><br />
      <% end %>
      <label>And if a landmark is not listed please add below: </label><br />
      <input type="text" name="landmark[name]" id="new_landmark" /><br />
      <input type="submit" value="Create New Figure" />
    </form>

  </body>
</html>
